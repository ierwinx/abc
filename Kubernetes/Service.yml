apiVersion: v1
kind: Service
metadata:
  name: serviciomicro2
  namespace: default
spec:
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  selector:
    app: microservicio2
  type: LoadBalancer